export default class Identity {
    constructor(username, identityAddress, privateKey) {
        this.username = username;
        this.identityAddress = identityAddress;
        this.privateKey = privateKey;
    }
}
